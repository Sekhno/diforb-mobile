import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.scss.starter',
  appName: 'Diforb Mobile',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
